# Mirrors akhilaaa3/Jev-Omni's requirements.txt at the pinned revision,
# minus the audio/video extras (opencv, soundfile, librosa): this model
# only serves image questions. hf_transfer speeds up the boot-time
# weight download.
torch==2.10.0
torchvision==0.25.0
transformers==5.17.0
accelerate
huggingface_hub[hf_transfer]
safetensors
numpy
Pillow
