{
  "name": "aura",
  "version": "1.0.0",
  "description": "Aura — Low-Latency Haptic & Audio Spatial Guidance for the Visually Impaired. A zero-install mobile web app that turns an Android phone into a closed-loop spatial guidance engine using Cerebras-hosted Gemma vision inference. Runs on Cloudflare Workers (Hono).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "predev": "node scripts/gen-icons.js",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "predeploy": "node scripts/gen-icons.js",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  },
  "license": "GPL-3.0"
}
